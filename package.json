{
  "name": "etokenize-contracts",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "git@github.com:eTokenize/server.git",
  "author": "eTokenize",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "truffle compile",
    "start": "ganache-cli --quiet --gasLimit 10000000 --noVMErrorsOnRPCResponse -p 8545 -m \"concert load couple harbor equip island argue ramp clarify fence smart topic\"",
    "migrate": "truffle migrate --reset --network development && npm run setupaccounts",
    "setupaccounts": "truffle exec --network development './scripts/setup_contracts.js'",
    "test": "truffle test --development"
  },
  "dependencies": {
    "ganache-cli": "^6.2.3",
    "openzeppelin-solidity": "^2.0.0",
    "truffle": "^4.1.14",
    "@ensdomains/ens": "^0.1.1",
    "eth-ens-namehash": "^2.0.8"
  }
}